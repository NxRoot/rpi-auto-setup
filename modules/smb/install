#!/bin/sh
root=$1
rpias=$2
value=$3

sudo rm -rf $root/shared
cd $rpias
mkdir $root/shared
sudo chmod -R 777 $root/shared
sudo apt install -y samba samba-common-bin
sudo cat _smb.txt >> /etc/samba/smb.conf