#!/bin/sh

REPLACE_LINE() {
    content=$(<$1)
    content=${content//$2/$3}
    echo "$content" > $1
}

RESTORE() {
    if [ -f "$1.orig" ]; then
        sudo cp $1.orig $1
    elif [ -f "$1" ]; then
        sudo cp $1 $1.orig
    fi
}